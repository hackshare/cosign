### What this changes


### Why


### Checklist

- [ ] Tests pass (`cargo test`, Xcode tests) and CI is green
- [ ] `rustfmt` + `clippy` and `swiftformat` + `swiftlint` are clean
- [ ] No secrets, keys, or `.env` files committed
- [ ] User-facing copy goes through the copy helpers (if applicable)
